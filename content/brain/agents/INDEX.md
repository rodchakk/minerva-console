# Agents — Index

Long-form agent definitions live in this folder, one file per agent. Each file is referenced from the matching entry in `content/brain/registries/agents.json` via the `path` field.

Agent definitions describe purpose, allowed tools, refusal rules, and the prompts the agent uses. Promotion to `approved` status requires a human review.
